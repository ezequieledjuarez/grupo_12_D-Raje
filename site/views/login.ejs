<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body>
    <%- include('partials/header') %> 
    <div id= "formulario">
        <form action="" method="POST">
            <input type="email"
            class = "correo" placeholder="Correo" name="correo"
            <%= (typeof old != 'undefined' && old.correo && !errores.correo)?"is-valid":
            (typeof errores != 'undefined' && errores.correo)?"is-invalid":'' %>" 
            value="<%= (typeof old != 'undefined' && old.correo)?old.correo:'' %>">
            <small class="text-danger"><%= (typeof errores != 'undefined' && errores.correo)?errores.correo.msg: "" %> </small>

            <input type="password" class = "password" placeholder="Contraseña" name="password"
            <%= (typeof errores != 'undefined' && errores.password)?"is-invalid":'' %>">
            <small class="text-danger">
                <%= (typeof errores != 'undefined' && errores.password)?errores.password.msg: "" %> 
            </small>
            <input type="submit" value="Ingresar">
            <div class="form-group form-check">
                <input type="checkbox" class="form-check-input" id="exampleCheck1" name="recordar">
                <label class="form-check-label" for="exampleCheck1">Recordarme</label>
            </div>
            <a href="/users/register">¿No tienes cuenta? Registrate</a>
        </form>
    </div>
    <%- include('partials/footer') %> 
</body>
</html>